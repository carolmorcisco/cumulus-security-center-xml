services:
  - type: web
    name: cumulus-security-center
    runtime: python
    buildCommand: python -m compileall app.py
    startCommand: python app.py
    healthCheckPath: /health
    envVars:
      - key: SPECIALIST_EXTENSION
        sync: false
      - key: PUBLIC_BASE_URL
        sync: false
